const CACHE='baby-playbook-v2-20260908-mobile';
const ASSETS=['./','./index.html','./styles.css','./mobile.css','./app.js','./core.mjs','./data.js','./faith.js','./manifest.webmanifest','./icon.svg','../assets/all-apps.css','../assets/mobile.css','../assets/chevron-down.svg'];
self.addEventListener('install',event=>event.waitUntil(caches.open(CACHE).then(cache=>cache.addAll(ASSETS))));
self.addEventListener('activate',event=>event.waitUntil(caches.keys().then(keys=>Promise.all(keys.filter(key=>key.startsWith('baby-playbook-')&&key!==CACHE).map(key=>caches.delete(key)))).then(()=>self.clients.claim())));
self.addEventListener('fetch',event=>{const url=new URL(event.request.url);const sharedAssets=['../assets/all-apps.css','../assets/mobile.css','../assets/chevron-down.svg'].map(path=>new URL(path,self.registration.scope).pathname);if(event.request.method!=='GET'||url.origin!==self.location.origin||(!url.pathname.startsWith(new URL(self.registration.scope).pathname)&&!sharedAssets.includes(url.pathname)))return;event.respondWith(caches.open(CACHE).then(async cache=>{const cached=await cache.match(event.request,{ignoreSearch:true});if(cached)return cached;try{return await fetch(event.request);}catch(error){if(event.request.mode==='navigate')return await cache.match('./index.html');throw error;}}));});
